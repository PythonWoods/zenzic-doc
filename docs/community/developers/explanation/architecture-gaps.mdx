---
sidebar_label: "Architectural Gaps"
sidebar_position: 5
description: "Known architectural gaps, technical debt, and planned improvements."
---

{/* SPDX-FileCopyrightText: 2026 PythonWoods <dev@pythonwoods.dev> */}
{/* SPDX-License-Identifier: Apache-2.0 */}

# Zenzic - Architectural Gaps & Technical Debt

> *"What is not documented, does not exist; what is documented poorly, is an ambush."*
>
> This document tracks architectural gaps and technical debt identified during development, which require resolution before specific milestones (like rc1).

---

## Target: v0.5.0rc1 (The Bastion)

### 1. Versioning Automation (Noxfile)

**Identified in:** v0.5.0a4 (`fix/sentinel-hardening`)
**Component:** `noxfile.py`
**Description:** The noxfile currently only supports `patch`, `minor`, and `major` bumps. During alpha/beta iterations, it is not possible to execute a prerelease bump directly via the automation framework (`nox -s bump -- prerelease`).
**Required Action:** The noxfile must be updated to extract and support pre-release tags (bumping `pre_l` and `pre_n`) by properly interfacing with `bump-my-version`, enabling rapid iteration of testing releases without circumventing automation.

### 2. Security Pipeline Coverage (CLI Integration)

**Identified in:** v0.5.0a4 (`fix/sentinel-hardening`)
**Component:** `zenzic/cli.py`
**Description:** The scanner and reporter now have complete mutation tests safeguarding the effectiveness of the Shield (The Sentinel's Trial). However, the silencer mutant (`findings.append(...) -> pass`) within `cli.py` is not covered by the current suite because it bypasses the CLI to interface with the proxy.
**Required Action:** An end-to-end (e2e) test that triggers the full CLI and verifies the exit with code 2 and the presence of the reporter to ensure the routing is not vulnerable to amnesia (Commit 4b or later).

---

## Resolved

### ZRT-005 — Bootstrap Paradox

**Identified in:** v0.5.0a3
**Component:** `zenzic/cli.py`, `zenzic/core/scanner.py`
**Description:** `zenzic init` crashed with a configuration error when invoked in an empty directory (no existing `zenzic.toml`). The bootstrap command incorrectly assumed a valid project context was already present before it could be created.
**Resolution:** Implemented a two-phase initialization sequence: (1) write `zenzic.toml` via a template isolated from the context resolver, (2) start the validation cycle only if the configuration file already exists. The resolver now tolerates an empty directory and delegates bootstrapping to the `init` command. Verified via Genesis Test: `zenzic init` in a completely empty directory correctly generates `zenzic.toml` with the commented Shield block visible.
**Closed in:** v0.5.0a4 (`fix/sentinel-hardening`) — commit `38be6f1`

### CI-001 — Node.js 24 / npx Non-Interactive Prompt (Cloudflare Pages)

**Identified in:** v0.5.0a4 (`fix/v050a4-infra-alignment`)
**Component:** `.github/workflows/deploy-docs.yml`
**Description:** `cloudflare/wrangler-action@v3` (latest stable) calls `npx wrangler`
without the `--yes` flag. npm 10+ on Node.js 24 GitHub Actions runners blocks
non-interactive package installation, aborting the deploy with:
`"npx canceled due to missing packages and no YES option: [wrangler@4.81.0]"`.
**Resolution:** Pre-install `wrangler@latest` globally via `npm install -g` before the
action step. npx finds the binary already in PATH and skips the interactive download
entirely. `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` added to suppress the Node.js 20
deprecation warning. Remove the pre-install step only when `wrangler-action` ships a
release that passes `--yes` to npx natively.
**Closed in:** v0.5.0a4 (`fix/v050a4-infra-alignment`)

### BRAND-001 — Brand System Static Asset (D096)

**Identified in:** v0.7.0 (`release/v0.7.0`)
**Component:** `static/assets/brand/zenzic-brand-system.html`, `docs/community/brand-kit.mdx`
**Description:** The brand kit page lacked a machine-readable, human-navigable reference
document that engineers and designers could open without a running Docusaurus server.
A standalone HTML file (`zenzic-brand-system.html`) was required, but Docusaurus's
`onBrokenLinks: 'throw'` rejects links to static HTML assets via normal relative paths.
**Resolution:** Created `static/assets/brand/zenzic-brand-system.html` as a self-contained
hardware-manual-style document (Sentinel Palette, SVG logos, social card preview, typography,
badge visuals, usage laws). Linked from `brand-kit.mdx` (EN + IT) using the `pathname:///`
prefix, which bypasses Docusaurus link validation for non-page static assets while keeping
the URL resolvable in production. Font stack aligned to site standard: Inter + JetBrains Mono.
**Closed in:** v0.7.0 — commit `1603dc4`

### BRAND-002 — Obsidian Lead Alignment (D097)

**Identified in:** v0.7.0 (`release/v0.7.0`)
**Component:** `static/assets/social/social-card.svg`, `static/assets/brand/zenzic-brand-system.html`, `src/css/custom.css`
**Description:** Three distinct background values (`#0d1117`, `#09090b`, `#111118`) were
used across social cards, the brand system HTML, and the homepage CSS — resulting in a
visually inconsistent "Obsidian" surface that varied by 3–5% luminance across surfaces.
**Resolution:** `#09090b` (Obsidian Lead) adopted as the single canonical background across
all dark surfaces. Changes: `social-card.svg` background `#0d1117` → `#09090b`, circle
`#161b22` → `#111118`; `brand-system.html` CSS vars `--obsidian` → `#09090b`,
`--slate` → `#0f0f13`, `--surface` → `#111118`; inline social SVG inside `brand-system.html`
aligned identically. Palette swatch "Obsidian Lead #09090b · Primary BG" added.
Social card PNGs (dark + light, 1200×630) regenerated via `cairosvg`.
**Closed in:** v0.7.0 — commits `1224309`, `070021d`

### INFRA-001 — Brand PNG Pipeline: No Subprocess (D097 Follow-up)

**Identified in:** v0.7.0 (`release/v0.7.0`)
**Component:** `scripts/bump-version.sh`, `scripts/build-assets.js`
**Description:** The `bump-version.sh` script regenerated only social card PNGs after a
version bump, leaving the four brand PNGs (`icon-512`, `nav-dark`, `nav-light`, `wordmark`)
stale. Additionally, `brand-kit.zip` was rebuilt exclusively by `build-assets.js` (Node.js /
AdmZip) during `npm run build` — meaning a plain version bump without a full Docusaurus
build produced a zip with outdated assets. This also violated Pillar 2 (No Subprocesses):
the bump pipeline spawned a Node.js process implicitly.
**Resolution:** `bump-version.sh` step 9 expanded to a `BRAND_PNG_MANIFEST`-driven loop
using `cairosvg` (pure Python): all 6 PNGs (4 brand + 2 social cards) are regenerated on
every bump. `BRAND_PNG_MIRRORS` dict handles `shutil.copy2` of brand PNGs to
`static/assets/brand/png/` (in-site serving for `brand-system.html`). Step 10 added:
`brand-kit.zip` rebuilt via Python `zipfile.ZipFile` + `ZIP_DEFLATED` — identical manifest
to `build-assets.js` (`static/brand/` → prefix `brand`, `static/assets/social/` → prefix
`social`). Zero subprocess calls. Dry-run verified: 6/6 SVGs → PNGs, zip 87 KB / 27 entries.
**Closed in:** v0.7.0 — commit `1cd712f`
