---
sidebar_position: 1
title: Examples Overview
description: A guided index of the Zenzic Z-code gallery — twenty self-contained violation scenarios that demonstrate the audit engine in action.
---

# Examples

Every example in the Zenzic repository is a **self-contained Z-code violation fixture**.
Clone it. Run `uvx zenzic check all`. Each example isolates one Z-code violation class —
the code shown is the minimum required to reproduce the reported exit code.

All examples are located in [`examples/`](https://github.com/PythonWoods/zenzic/tree/main/examples) in the Zenzic repository.

---

## Quick-Run Pattern {#quick-run}

Every example follows the same convention:

```bash
# From the repo root — no install required
cd examples/<z-code-dir>
uvx zenzic check all
```

Or launch the interactive gallery:

```bash
uvx zenzic lab            # gallery menu
uvx zenzic lab z101       # run the Z101 scenario
uvx zenzic lab all        # run all 20 scenarios
```

---

## Z-Code Gallery {#gallery}

### z101-broken-links — Link Integrity {#example-z101}

**Directory:** `examples/z1xx-links/z101-broken-links/`
**Engine:** `standalone`
**Z-Code:** Z101 `LINK_BROKEN`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ 1 error — exit 1</span></ZenzicTerminal>

Demonstrates Zenzic detecting Markdown links that point to files that do not exist.
The fixture contains `docs/index.md` with two broken links — one to a missing file
(`missing.md`) and one to a file inside a missing subdirectory (`guide/setup.md`).
Both trigger Z101 findings.

**Start here if:** you want to understand how Zenzic validates internal link references.

---

### z201-credentials — Credential Scanner {#example-z201}

**Directory:** `examples/z2xx-security/z201-credentials/`
**Engine:** `standalone`
**Z-Code:** Z201 `CREDENTIAL_SECRET`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ SECURITY BREACH — exit 2</span></ZenzicTerminal>

Contains a synthetic AWS access key embedded in `docs/setup.md`. Zenzic fires the
**security_breach** severity finding — the non-suppressible alert for a leaked credential.
Exit code 2 is expected and correct.

:::danger[This example exits with code 2 by design]
`z201-credentials` contains an intentionally-placed synthetic credential for credential
scanner testing. Do **not** treat exit code 2 from this example as a real security incident.
:::

**Start here if:** you want to see how the credential scanner detects and reports secrets.

---

### z405-unused-assets — Asset Integrity {#example-z405}

**Directory:** `examples/z4xx-topology/z405-unused-assets/`
**Engine:** `standalone`
**Z-Code:** Z405 `UNUSED_ASSET`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ 1 error — exit 1</span></ZenzicTerminal>

Contains `assets/banner.png` — an image file that exists on disk but is never linked
from any Markdown document. Zenzic detects the orphan asset and reports Z405.

**Start here if:** you want to validate that every asset in your project is actually used.

---

### z601-brand-obsolescence — Brand Obsolescence {#example-z601}

**Directory:** `examples/z6xx-brand/z601-brand-obsolescence/`
**Engine:** `standalone`
**Z-Code:** Z601 `BRAND_OBSOLESCENCE`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ 1 error — exit 1</span></ZenzicTerminal>

The `.zenzic.toml` declares `OldPlatform` as an obsolete brand name. The fixture's
`docs/index.md` mentions `OldPlatform` — triggering Z601 findings on every occurrence.

**Start here if:** you maintain a governance list of deprecated product names or brands
that must be eliminated from documentation.

---

### z602-i18n-parity — i18n Parity {#example-z602}

**Directory:** `examples/z6xx-brand/z602-i18n-parity/`
**Engine:** `standalone`
**Z-Code:** Z602 `I18N_PARITY`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ 1 error — exit 1</span></ZenzicTerminal>

`docs/en/` contains two pages: `index.md` and `guide.md`. The Italian locale
`docs/it/` has only `index.md` — `guide.md` is missing. Zenzic detects the parity
gap and reports Z602.

**Start here if:** you maintain multilingual documentation and need to enforce that every
page translated in one locale also exists in all other configured locales.

---

### z102-anchor-missing — Anchor Integrity {#example-z102}

**Directory:** `examples/z1xx-links/z102-anchor-missing/`
**Engine:** `standalone`
**Z-Code:** Z102 `ANCHOR_MISSING`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ 1 error — exit 1</span></ZenzicTerminal>

`docs/index.md` links to `guide.md#nonexistent-section`. The target file exists but
has no `#nonexistent-section` heading. Zenzic's link validator detects the dead
fragment without any network request.

**Start here if:** you want to validate that fragment links resolve to real headings.

---

### z103-orphan-link — Orphan Link {#example-z103}

**Directory:** `examples/z1xx-links/z103-orphan-link/`
**Engine:** `zensical`
**Z-Code:** Z103 `ORPHAN_LINK`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ 1 error — exit 1</span></ZenzicTerminal>

`docs/guide.md` exists on disk but is not listed in `zensical.toml` nav. `index.md` links
to it, so the file is reachable via direct URL but invisible through site navigation.
Requires the zensical engine for VSM-aware link checking.

**Start here if:** you use zensical and need to detect links that bypass navigation structure.

---

### z105-absolute-path — Absolute Path {#example-z105}

**Directory:** `examples/z1xx-links/z105-absolute-path/`
**Engine:** `standalone`
**Z-Code:** Z105 `ABSOLUTE_PATH`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ 1 error — exit 1</span></ZenzicTerminal>

`docs/index.md` contains `[Guide](/guide)` — an absolute path starting with `/`. Absolute
paths break portability when a site is hosted in a subdirectory.

**Start here if:** your documentation must be host-path-portable (CDN subdirectory, GitHub Pages).

---

### z108-empty-link-text — Empty Link Text {#example-z108}

**Directory:** `examples/z1xx-links/z108-empty-link-text/`
**Engine:** `standalone`
**Z-Code:** Z108 `EMPTY_LINK_TEXT`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ 1 error — exit 1</span></ZenzicTerminal>

`docs/index.md` contains `[](guide.md)` — a link with an empty label. Empty link text
is inaccessible to screen readers and violates WCAG 2.1 §2.4.4.

**Start here if:** you want to enforce accessible link labeling across your documentation.

---

### z202-path-traversal — Path Traversal {#example-z202}

**Directory:** `examples/z2xx-security/z202-path-traversal/`
**Engine:** `standalone`
**Z-Code:** Z202 `PATH_TRAVERSAL`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ 1 error — exit 1</span></ZenzicTerminal>

`docs/index.md` contains `[Config](../../private/secret.txt)` — a link that escapes the
`docs/` root via `../..`. Z202 is non-suppressible. Exit code 1.

:::danger[This example exits with code 1 by design]
`z202-path-traversal` contains an intentional directory traversal link for scanner testing.
Do **not** suppress or ignore Z202 findings in production — they are non-suppressible by design.
:::

**Start here if:** you need to understand how Zenzic enforces the docs-root boundary.

---

### z204-forbidden-term — Policy Violation {#example-z204}

**Directory:** `examples/z2xx-security/z204-forbidden-term/`
**Engine:** `standalone`
**Z-Code:** Z204 `FORBIDDEN_TERM`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ SECURITY BREACH — exit 2</span></ZenzicTerminal>

`.zenzic.local.toml` declares `ProjectX` as a forbidden term. `docs/index.md` mentions it —
triggering the **POLICY VIOLATION DETECTED** banner (distinct from Z201's credential banner).
Z204 is non-suppressible. Exit code 2.

:::danger[This example exits with code 2 by design]
`z204-forbidden-term` contains intentional forbidden-term content for governance scanner testing.
The `.zenzic.local.toml` is committed for demonstration only — in production, it is git-ignored.
:::

**Start here if:** you need to enforce internal codenames or confidential terms never appear in docs.

---

### z301-dangling-ref — Dangling Reference {#example-z301}

**Directory:** `examples/z3xx-references/z301-dangling-ref/`
**Engine:** `standalone`
**Z-Code:** Z301 `DANGLING_REF`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ 1 error — exit 1</span></ZenzicTerminal>

`docs/index.md` uses `[Click here][missing-ref]` but no `[missing-ref]: url` definition
exists anywhere in the file. Zenzic's reference scanner flags the undefined identifier.

**Start here if:** you use reference-style links and want to catch typos in link IDs.

---

### z302-dead-def — Dead Definition {#example-z302}

**Directory:** `examples/z3xx-references/z302-dead-def/`
**Engine:** `standalone`
**Z-Code:** Z302 `DEAD_DEF`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ 1 error — exit 1</span></ZenzicTerminal>

`docs/index.md` contains an intentional dead-definition defect around the `setup` reference ID:
the reference definition pattern is present but never consumed by a reference-style link.
This orphan definition pattern signals hidden documentation debt.

**Start here if:** you want to keep your reference link tables free of unused entries.

---

### z303-duplicate-def — Duplicate Definition {#example-z303}

**Directory:** `examples/z3xx-references/z303-duplicate-def/`
**Engine:** `standalone`
**Z-Code:** Z303 `DUPLICATE_DEF`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ 1 error — exit 1</span></ZenzicTerminal>

`docs/index.md` contains an intentional duplicate-definition defect for the `api` reference ID.
When the same reference ID is declared more than once, Markdown renderers silently pick one URL,
creating invisible link drift.

**Start here if:** you want to enforce unique reference identifiers across documentation pages.

---

### z402-orphan-page — Orphan Page {#example-z402}

**Directory:** `examples/z4xx-topology/z402-orphan-page/`
**Engine:** `zensical`
**Z-Code:** Z402 `ORPHAN_PAGE`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ 1 error — exit 1</span></ZenzicTerminal>

`docs/secret.md` exists on disk but is not listed in `zensical.toml` nav. No visitor
navigating the site menu can reach it. Requires the zensical engine.

**Start here if:** you need to audit your documentation for pages that fell out of the nav.

---

### z403-missing-alt — Missing Alt Text {#example-z403}

**Directory:** `examples/z4xx-topology/z403-missing-alt/`
**Engine:** `standalone`
**Z-Code:** Z403 `MISSING_ALT`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ 1 error — exit 1</span></ZenzicTerminal>

`docs/index.md` contains `![](diagram.png)` — an image element with no alt text, violating
WCAG 2.1 §1.1.1. Zenzic flags this syntactically; no I/O on the image file is performed.

**Start here if:** you want to enforce accessible image alt text across your documentation.

---

### z501-placeholder — Placeholder Content {#example-z501}

**Directory:** `examples/z5xx-content/z501-placeholder/`
**Engine:** `standalone`
**Z-Code:** Z501 `PLACEHOLDER`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ 1 error — exit 1</span></ZenzicTerminal>

`docs/index.md` contains `TODO: content goes here` and `Coming soon!` — classic stub
patterns that signal unfinished pages published by accident.

**Start here if:** you want to catch stub or draft content before it reaches production.

---

### z502-short-content — Short Content {#example-z502}

**Directory:** `examples/z5xx-content/z502-short-content/`
**Engine:** `standalone`
**Z-Code:** Z502 `SHORT_CONTENT`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ 1 error — exit 1</span></ZenzicTerminal>

`docs/index.md` has only 8 words of prose — below the default 50-word minimum threshold.
The page lacks placeholder patterns, so only Z502 fires, not Z501.

**Start here if:** you want to enforce a minimum content density for published pages.

---

### z503-snippet-error — Snippet Error {#example-z503}

**Directory:** `examples/z5xx-content/z503-snippet-error/`
**Engine:** `standalone`
**Z-Code:** Z503 `SNIPPET_ERROR`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ 1 error — exit 1</span></ZenzicTerminal>

`docs/index.md` contains a fenced `python` block with a `SyntaxError` (`def compute_total(items =`).
Zenzic's `ast`-based snippet validator catches the invalid snippet at documentation-build time.

**Start here if:** you publish Python code examples and want to guarantee they are syntactically valid.

---

### z505-untagged-code-block — Untagged Code Block {#example-z505}

**Directory:** `examples/z5xx-content/z505-untagged-code-block/`
**Engine:** `standalone`
**Z-Code:** Z505 `UNTAGGED_CODE_BLOCK`
<ZenzicTerminal title="zenzic check all"><span className="text-rose-500 font-medium">✘ 1 error — exit 1</span></ZenzicTerminal>

`docs/index.md` contains a fenced code block with no language specifier (bare ` ``` `).
Zenzic's built-in `UntaggedCodeBlockRule` is always active — no configuration needed.

**Start here if:** you want to enforce language tags on every fenced code block.

---

## Feature-to-Example Matrix {#matrix}

| Z-Code | Violation Class | Example |
| :---: | :--- | :--- |
| Z101 | Broken internal links | [z101-broken-links](examples/z1xx-links/z101-broken-links) |
| Z102 | Fragment anchor not defined | [z102-anchor-missing](examples/z1xx-links/z102-anchor-missing) |
| Z103 | Link to nav-orphaned page | [z103-orphan-link](examples/z1xx-links/z103-orphan-link) |
| Z105 | Absolute path in link | [z105-absolute-path](examples/z1xx-links/z105-absolute-path) |
| Z108 | Empty link text | [z108-empty-link-text](examples/z1xx-links/z108-empty-link-text) |
| Z201 | Credential / secret detection | [z201-credentials](examples/z2xx-security/z201-credentials) |
| Z202 | Docs-root path traversal | [z202-path-traversal](examples/z2xx-security/z202-path-traversal) |
| Z204 | Forbidden governance term | [z204-forbidden-term](examples/z2xx-security/z204-forbidden-term) |
| Z301 | Dangling reference-style link | [z301-dangling-ref](examples/z3xx-references/z301-dangling-ref) |
| Z302 | Unused reference definition | [z302-dead-def](examples/z3xx-references/z302-dead-def) |
| Z303 | Duplicate reference definition | [z303-duplicate-def](examples/z3xx-references/z303-duplicate-def) |
| Z402 | Markdown page not in nav | [z402-orphan-page](examples/z4xx-topology/z402-orphan-page) |
| Z403 | Image missing alt text | [z403-missing-alt](examples/z4xx-topology/z403-missing-alt) |
| Z405 | Unreferenced asset on disk | [z405-unused-assets](examples/z4xx-topology/z405-unused-assets) |
| Z501 | Stub / TODO placeholder content | [z501-placeholder](examples/z5xx-content/z501-placeholder) |
| Z502 | Page below minimum word count | [z502-short-content](examples/z5xx-content/z502-short-content) |
| Z503 | Python snippet syntax error | [z503-snippet-error](examples/z5xx-content/z503-snippet-error) |
| Z505 | Untagged fenced code block | [z505-untagged-code-block](examples/z5xx-content/z505-untagged-code-block) |
| Z601 | Deprecated brand name in content | [z601-brand-obsolescence](examples/z6xx-brand/z601-brand-obsolescence) |
| Z602 | i18n locale file parity gap | [z602-i18n-parity](examples/z6xx-brand/z602-i18n-parity) |

---

## See Also {#see-also}

- [Architecture](../explanation/architecture) — Adapter vs Integration model.
- [Discovery & Exclusion](../explanation/discovery) — How the Layered Exclusion hierarchy works.
- [Checks Reference](../reference/checks) — All available `zenzic check` commands and their findings.
- [CLI Reference — lab](../reference/cli#lab) — Full documentation for `zenzic lab`.
