---
sidebar_label: "Lacune Architetturali"
sidebar_position: 5
description: "Lacune architetturali note, debito tecnico e miglioramenti pianificati."
---

{/* SPDX-FileCopyrightText: 2026 PythonWoods <dev@pythonwoods.dev> */}
{/* SPDX-License-Identifier: Apache-2.0 */}

# Zenzic - Architectural Gaps & Technical Debt

> *"Ciò che non è documentato, non esiste; ciò che è documentato male, è un'imboscata."*
>
> Questo documento traccia i gap architetturali e il debito tecnico identificati durante lo sviluppo, che necessitano di risoluzione prima di traguardi specifici (come la rc1).

---

## Target: v0.5.0rc1 (The Bastion)

### 1. Automazione del Versioning (Noxfile)

**Identificato in:** v0.5.0a4 (`fix/sentinel-hardening`)
**Componente:** `noxfile.py`
**Descrizione:** Il noxfile attualmente supporta solo bump di `patch`, `minor` e `major`. Durante le iterazioni alpha/beta, non è possibile eseguire il bump prerelease direttamente tramite il framework di automazione (`nox -s bump -- prerelease`).
**Azione Richiesta:** Il noxfile deve essere aggiornato per estrarre e supportare la gestione dei tag alpha/beta pre-release (bump `pre_l` e `pre_n`) interfacciandosi correttamente con `bump-my-version`, per permettere l'iterazione rapida delle release di testing senza bypassare l'automazione.

### 2. Copertura della Pipeline di Sicurezza (Integrazione CLI)

**Identificato in:** v0.5.0a4 (`fix/sentinel-hardening`)
**Componente:** `zenzic/cli.py`
**Descrizione:** Lo scanner e il reporter dispongono ora di mutation test completi che proteggono l'efficacia dello Shield (The Sentinel's Trial). Tuttavia, la mutazione del silenziatore (`findings.append(...) -> pass`) all'interno di `cli.py` non viene coperta dalla suite attuale perché essa salta la CLI per interfacciarsi con il proxy.
**Azione Richiesta:** Un test end-to-end (e2e) che attivi l'intera CLI e verifichi l'uscita con exit code 2 e la presenza del reporter per assicurare che il routing non sia vulnerabile ad amnesie (Commit 4b o successivi).

---

## Risolti

### ZRT-005 — Bootstrap Paradox

**Identificato in:** v0.5.0a3
**Componente:** `zenzic/cli.py`, `zenzic/core/scanner.py`
**Descrizione:** `zenzic init` crashava con un errore di configurazione quando veniva invocato in una directory vuota (senza `zenzic.toml` esistente). Il comando di bootstrap assumeva erroneamente che un contesto di progetto valido fosse già presente prima ancora di crearlo.
**Risoluzione:** Implementata una sequenza di inizializzazione a due fasi: (1) scrittura del file `zenzic.toml` tramite template isolato dal resolver di contesto, (2) avvio del ciclo di validazione solo se il file di configurazione esiste già. Il resolver ora tollera la directory vuota e delegamente l'amorcage al comando `init`. Verificato con il Genesis Test: `zenzic init` in directory completamente vuota genera correttamente `zenzic.toml` con il blocco Shield commentato.
**Chiuso in:** v0.5.0a4 (`fix/sentinel-hardening`) — commit `38be6f1`

### CI-001 — Node.js 24 / npx Prompt Non Interattivo (Cloudflare Pages)

**Identificato in:** v0.5.0a4 (`fix/v050a4-infra-alignment`)
**Componente:** `.github/workflows/deploy-docs.yml`
**Descrizione:** `cloudflare/wrangler-action@v3` (ultima versione stabile) invoca
`npx wrangler` senza il flag `--yes`. npm 10+ sui runner GitHub con Node.js 24
blocca l'installazione non interattiva dei pacchetti, interrompendo il deploy con:
`"npx canceled due to missing packages and no YES option: [wrangler@4.81.0]"`.
**Risoluzione:** Pre-installazione globale di `wrangler@latest` tramite `npm install -g`
prima dello step dell'action. npx trova il binario già nel PATH e salta il download
interattivo. `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` aggiunto per sopprimere il
warning di deprecazione di Node.js 20. Rimuovere lo step di pre-installazione solo
quando `wrangler-action` rilascia una versione che passa `--yes` a npx nativamente.
**Chiuso in:** v0.5.0a4 (`fix/v050a4-infra-alignment`)

### BRAND-001 — Asset Statico Brand System (D096)

**Identificato in:** v0.7.0 (`release/v0.7.0`)
**Componente:** `static/assets/brand/zenzic-brand-system.html`, `docs/community/brand-kit.mdx`
**Descrizione:** La pagina del brand kit mancava di un documento di riferimento leggibile
sia dalle macchine che dagli ingegneri, consultabile senza un server Docusaurus in esecuzione.
Era richiesto un file HTML standalone (`zenzic-brand-system.html`), ma `onBrokenLinks: 'throw'`
di Docusaurus rifiuta i link ad asset HTML statici tramite percorsi relativi ordinari.
**Risoluzione:** Creato `static/assets/brand/zenzic-brand-system.html` come documento
standalone in stile "hardware manual" (Sentinel Palette, logo SVG, anteprima social card,
tipografia, badge, leggi d'uso). Linkato da `brand-kit.mdx` (EN + IT) usando il prefisso
`pathname:///`, che bypassa la validazione dei link di Docusaurus per gli asset statici
non-pagina mantenendo la URL risolvibile in produzione. Font stack allineato allo standard
del sito: Inter + JetBrains Mono.
**Chiuso in:** v0.7.0 — commit `1603dc4`

### BRAND-002 — Allineamento Obsidian Lead (D097)

**Identificato in:** v0.7.0 (`release/v0.7.0`)
**Componente:** `static/assets/social/social-card.svg`, `static/assets/brand/zenzic-brand-system.html`, `src/css/custom.css`
**Descrizione:** Tre valori di background distinti (`#0d1117`, `#09090b`, `#111118`) erano
usati su social card, brand system HTML e CSS homepage — producendo una superficie "Obsidian"
visivamente incoerente, con variazioni di luminanza del 3–5% tra le superfici.
**Risoluzione:** `#09090b` (Obsidian Lead) adottato come colore di background canonico unico
su tutte le superfici dark. Modifiche: `social-card.svg` background `#0d1117` → `#09090b`,
cerchio `#161b22` → `#111118`; `brand-system.html` var CSS `--obsidian` → `#09090b`,
`--slate` → `#0f0f13`, `--surface` → `#111118`; SVG social inline dentro `brand-system.html`
allineato identicamente. Aggiunta swatch palette "Obsidian Lead #09090b · Primary BG".
PNG delle social card (dark + light, 1200×630) rigenerate via `cairosvg`.
**Chiuso in:** v0.7.0 — commit `1224309`, `070021d`

### INFRA-001 — Pipeline PNG Brand: No Subprocess (D097 Follow-up)

**Identificato in:** v0.7.0 (`release/v0.7.0`)
**Componente:** `scripts/bump-version.sh`, `scripts/build-assets.js`
**Descrizione:** Lo script `bump-version.sh` rigenerava solo i PNG delle social card dopo
un bump di versione, lasciando i quattro PNG brand (`icon-512`, `nav-dark`, `nav-light`,
`wordmark`) potenzialmente obsoleti. Inoltre, `brand-kit.zip` veniva ricostruito
esclusivamente da `build-assets.js` (Node.js / AdmZip) durante `npm run build` — il che
significava che un semplice version bump senza una build Docusaurus completa produceva uno
zip con asset datati. Questo violava anche il Pilastro 2 (No Subprocess): la pipeline di
bump avviava implicitamente un processo Node.js.
**Risoluzione:** Lo step 9 di `bump-version.sh` espanso in un loop guidato da
`BRAND_PNG_MANIFEST` che usa `cairosvg` (Python puro): tutti i 6 PNG (4 brand + 2 social
card) vengono rigenerati ad ogni bump. Il dict `BRAND_PNG_MIRRORS` gestisce `shutil.copy2`
dei PNG brand verso `static/assets/brand/png/` (serving in-site per `brand-system.html`).
Aggiunto step 10: `brand-kit.zip` ricostruito via `zipfile.ZipFile` + `ZIP_DEFLATED` Python
stdlib — manifest identico a `build-assets.js` (`static/brand/` → prefisso `brand`,
`static/assets/social/` → prefisso `social`). Zero subprocess. Dry-run verificato:
6/6 SVG → PNG, zip 87 KB / 27 voci.
**Chiuso in:** v0.7.0 — commit `1cd712f`
