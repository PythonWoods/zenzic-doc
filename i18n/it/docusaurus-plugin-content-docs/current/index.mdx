---
sidebar_position: 0
sidebar_label: "Panoramica"
---

# La tua documentazione, completamente protetta

**Zenzic garantisce sicurezza in build-time e tranquillità operativa — rilevando link rotti,
credenziali esposte e pagine orfane prima che raggiungano la produzione. Nessuna configurazione
richiesta per iniziare. Nessun build engine invocato. Nessuna sorpresa il venerdì.**

---

## Provalo subito — zero installazione

```bash
uvx zenzic lab
```

`zenzic lab` lancia una vetrina interattiva di ogni scanner in azione: rilevamento credenziali,
link rotti, pagine orfane, Blood Sentinel per la protezione da path-traversal. Eseguilo su
fixture preconfezionate e osserva i risultati in tempo reale.

**L'audit completo in un secondo — senza installazione, senza configurazione:**

```bash
uvx zenzic check all .
```

→ **[Scopri di più nel tutorial Interactive Lab](./tutorials/first-audit#step-2-siege)**

---

## Il Sentinel Dashboard — 100/100

Ecco come appare il successo:

<SentinelOutput variant="clean" showFrame />

`100/100` non è una stima. È una **prova matematica** che la tua documentazione
non ha problemi strutturali, nessuna credenziale esposta, nessun errore di navigazione.
Qualsiasi punteggio inferiore è un diagnostico preciso e azionabile — non un avviso generico.

> **Pronto a raggiungere il 100/100?** Esegui `uvx zenzic score` per vedere dove sei oggi.

---

## Smetti di decifrare i log. Inizia a risolvere i problemi.

**Prima di Zenzic** — un fallimento CI standard:

```text
[14:23:01] BUILD FAILED
[14:23:01]   error: Cannot read properties of undefined (reading 'path')
[14:23:01]   at DocusaurusContentDocs.<anonymous> (content-docs.js:212:14)
[14:23:01]   ... 47 righe omesse ...
[14:23:01] Process exited with code 1
```

Cosa si è rotto? Quale file? Quale riga? Il build engine sa che ha fallito — ma non perché.

**Con Zenzic** — il Sentinel Report:

<SentinelOutput variant="findings" showFrame />

**Tre righe. Tre azioni. Zero ambiguità.**

Il Sentinel Report indica file, riga, codice `Zxxx` e soluzione — prima ancora che
il build inizi. Nessuna archeologia dei log, nessuna lettura di stack trace.

---

## Shield — L'ultima linea di difesa contro la Crisi Nera

Una chiave API esposta nella tua documentazione non è un problema di contenuto.
È un incidente di sicurezza.

<SentinelOutput variant="breach" showFrame />

Lo **Shield** di Zenzic scansiona ogni riga di ogni file — incluso il frontmatter YAML e
i blocchi di codice — prima che qualsiasi altro controllo venga eseguito. Se viene rilevata
una credenziale:

- La pipeline **si blocca incondizionatamente** — l'exit code 2 non può essere soppresso da alcun flag
- La credenziale è **mascherata** nel report (mai stampata per intero)
- La correggi **prima che entri nella git history** — niente BFG Repo Cleaner, niente rotazioni
  d'emergenza a mezzanotte, nessun post-mortem di incidente

Questo non è un controllo lint. È un **gate di sicurezza non negoziabile**.

→ **[Come agire quando Shield blocca un segreto](./how-to/configure-ci-cd#shield-recovery)**

---

## Quattro Punti d'Ingresso

| | |
| :--- | :--- |
| **[Tutorial →](./tutorials/first-audit)** | Da zero a un audit superato in 3 minuti. |
| **[How-to →](./how-to/configure-ci-cd)** | Pubblica con fiducia usando GitHub Actions. |
| **[Reference →](./reference/cli)** | Ogni flag, ogni codice `Zxxx`, ogni opzione. |
| **[Explanation →](./explanation/health-metrics)** | Perché 100/100 è l'unico numero che conta. |

---

## Enterprise Ready — Integrazione Nativa con GitHub

```yaml
- uses: PythonWoods/zenzic-action@v1
  with:
    format: sarif
    upload-sarif: "true"
```

I finding appaiono come **annotazioni inline nelle Pull Request** e nel **tab Security** —
nessun parsing di log, nessuno script personalizzato, nessun post-processing. L'ufficiale
[`PythonWoods/zenzic-action`](https://github.com/PythonWoods/zenzic-action) gestisce
installazione, esecuzione e upload SARIF 2.1.0 in un singolo step del workflow.

Verificato cross-platform: Ubuntu · Windows · macOS · Python 3.11–3.13.

---

*"The Code is Law. The Documentation is Truth. The Sentinel is vigilant."*
